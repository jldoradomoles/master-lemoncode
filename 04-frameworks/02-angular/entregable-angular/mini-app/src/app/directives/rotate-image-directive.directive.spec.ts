import { RotateImageDirectiveDirective } from './rotate-image-directive.directive';

describe('RotateImageDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new RotateImageDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
